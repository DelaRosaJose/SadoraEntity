<UserControl x:Class="Sadora.CustomElements.UscTextboxGeneral"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Name="root"
             mc:Ignorable="d" Focusable="True" GotFocus="Root_GotFocus" IsTabStop="True">
    <Grid>
        <Border TextElement.FontSize="15" Margin="{Binding MarginBorder, ElementName=root}" BorderBrush="{DynamicResource PrimaryHueDarkBrush}" BorderThickness="2 0 0 2" CornerRadius="5">
            <StackPanel Orientation="Vertical">
                <TextBlock Margin="10 5 0 0" Text="{Binding Title, ElementName=root}" FontWeight="Bold"/>
                <TextBox x:Name="MainText" Height="{Binding HeightLabel, ElementName=root}" Text="{Binding Text, ElementName=root}" Padding="0" MaxLength="{Binding MaxLength, ElementName=root}" BorderThickness="1" BorderBrush="{x:Null}" 
                         Style="{DynamicResource MaterialDesignTextBoxBase}" Background="#4CEDEDED" PreviewKeyDown="MainText_PreviewKeyDown" KeyUp="MainText_KeyUp"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
